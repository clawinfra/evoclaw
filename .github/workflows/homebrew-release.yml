# Homebrew formula updates are now handled inside "Build Release Packages"
# (the homebrew job, which runs after macos-dmg completes).
# This file is a no-op stub kept for git history only.
name: Update Homebrew Tap (disabled)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is disabled — see release-packages.yml homebrew job'
        required: false
        default: 'no-op'

jobs:
  noop:
    name: No-op (disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Homebrew updates moved to Build Release Packages workflow"
