# Homebrew formula updates are now handled as part of the
# "Build Release Packages" workflow (homebrew job, needs: [macos-dmg]).
# This file is intentionally a no-op stub to avoid breaking the file history.
name: Update Homebrew Tap (disabled)
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled — see release-packages.yml homebrew job'
        required: false
jobs: {}
