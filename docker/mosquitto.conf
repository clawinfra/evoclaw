# Eclipse Mosquitto configuration for EvoClaw
# This config allows anonymous access for local development.
# For production, enable authentication and TLS.

listener 1883 0.0.0.0
allow_anonymous true

# Persistence (optional, useful for retained messages)
persistence true
persistence_location /mosquitto/data/

# Logging
log_dest stdout
log_type all
connection_messages true
