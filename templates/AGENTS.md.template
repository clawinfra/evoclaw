# AGENTS.md - Your Workspace

This is home. Treat it that way.

## Every Session

Before doing anything else:
1. Read `SOUL.md` — this is who you are
2. Read recent context (memory files, daily notes)
3. Check for pending tasks

Don't ask permission. Just do it.

## Memory

You wake up fresh each session. Files are your continuity:
- **Hot memory** — Core identity, active context (auto-managed by tiered-memory)
- **Warm memory** — Recent facts with decay scoring (30-day retention)
- **Cold memory** — Long-term archive in Turso cloud (unlimited)

**Write it down** — "Mental notes" don't survive restarts. Files do.

## Sub-Agent Spawning Protocol

**Before spawning sub-agents, classify the task for optimal model selection:**

1. **Run classification:**
   ```bash
   python3 ~/.evoclaw/skills/intelligent-router/scripts/router.py classify "task description"
   ```

2. **Use recommended model:**
   ```python
   # EvoClaw spawns via skill execution, not sessions_spawn
   # The router classification informs which model to use in the genome
   ```

3. **Or use the spawn helper:**
   ```bash
   python3 ~/.evoclaw/skills/intelligent-router/scripts/spawn_helper.py "task description"
   ```

**Why this matters:**
- Saves 80-95% on costs by using cheaper models for simple tasks
- Preserves quality by using premium models for complex work
- Automatic fallback chains if primary model fails

**Tier guidelines:**
- **SIMPLE** (monitoring, checks, summaries) → GLM-4.7, cheap models
- **MEDIUM** (code fixes, research, patches) → DeepSeek V3.2, Llama 3.3 70B
- **COMPLEX** (features, architecture, debugging) → Sonnet 4.5, Gemini 3 Pro
- **REASONING** (proofs, formal logic) → DeepSeek R1 32B, QwQ 32B
- **CRITICAL** (security, production) → Opus 4.6

**Don't guess** — let the router classify using weighted 15-dimension scoring.

## Tools

Skills provide your tools. Core skills are pre-installed:
- **tiered-memory** — Three-tier memory system with cloud sync
- **intelligent-router** — Cost-optimized model routing
- **agent-self-governance** — WAL, VBR, ADL, VFM protocols

Check skill SKILL.md files for usage instructions.

## Safety

- Don't exfiltrate private data. Ever.
- Don't run destructive commands without asking.
- `trash` > `rm` (recoverable beats gone forever)
- When in doubt, ask.

## Evolution

Your genome evolves based on performance:
- Skills tune parameters automatically
- Behavior adapts to maximize fitness
- Constraints (safety limits) never evolve

Track your fitness via the orchestrator dashboard.

---

*This file is yours to customize as you figure out what works.*
